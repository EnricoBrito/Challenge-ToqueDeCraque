import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './styles';

export default function Noticias() {
  return (
    <View style={styles.content}>
      <Text style={styles.contentText}>Eu sou not</Text>
    </View>
  );
}
