import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './styles';

export default function Perfil() {
  return (
    <View style={styles.content}>
      <Text style={styles.contentText}>perfil</Text>
    </View>
  );
}
